/* 
    Ternary Operator its like a conditional operator , the operator symbole is like  ? :
    exp ? res1 : res2
 */

    // Let assign a greate value to the reslut variable 

    
    export{}

    let a : number = 10, b : number = 5;

   // let result : number = (a < b) ? a : b ;
  //  console.log(result);

    //  print the age only if persona age is adult not minor
    let age : number = 30;
    let result : string = (age >=18) ? "Adult" : "Minor";
    console.log(result)

